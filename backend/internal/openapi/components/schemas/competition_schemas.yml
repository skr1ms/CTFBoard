schemas:
  response.CompetitionResponse:
    properties:
      end_time:
        type: string
      freeze_time:
        type: string
      id:
        type: integer
      is_paused:
        type: boolean
      is_public:
        type: boolean
      keep_scoreboard_frozen_after_end:
        type: boolean
      mode:
        type: string
        description: Participation mode for the competition.
        enum: [teams_only, solo_only]
      name:
        type: string
      paused_at:
        type: string
      start_time:
        type: string
      status:
        type: string
      allow_team_switch:
        type: boolean
      min_team_size:
        type: integer
      max_team_size:
        type: integer
      flag_regex:
        type: string
    type: object
  response.CompetitionStatusResponse:
    properties:
      end_time:
        type: string
      freeze_time:
        type: string
      keep_scoreboard_frozen_after_end:
        type: boolean
      allow_team_switch:
        type: boolean
      min_team_size:
        type: integer
      max_team_size:
        type: integer
      mode:
        type: string
        description: Current participation mode for the competition.
        enum: [teams_only, solo_only]
      name:
        type: string
      paused_at:
        type: string
      start_time:
        type: string
      status:
        type: string
      submission_allowed:
        type: boolean
    type: object
  Competition:
    properties:
      id:
        type: integer
      name:
        type: string
      start_time:
        type: string
      end_time:
        type: string
      freeze_time:
        type: string
      is_paused:
        type: boolean
      mode:
        type: string
        description: Competition participation mode.
        enum: [teams_only, solo_only]
    type: object
  request.UpdateCompetitionRequest:
    properties:
      allow_team_switch:
        type: boolean
      clear_end_time:
        type: boolean
      clear_freeze_time:
        type: boolean
      end_time:
        format: date-time
        type: string
      flag_regex:
        type: string
      freeze_time:
        format: date-time
        type: string
      is_paused:
        type: boolean
      is_public:
        type: boolean
      keep_scoreboard_frozen_after_end:
        type: boolean
      max_team_size:
        type: integer
        minimum: 1
        x-oapi-codegen-extra-tags:
          validate: "omitempty,min=1"
      min_team_size:
        type: integer
        minimum: 1
        x-oapi-codegen-extra-tags:
          validate: "omitempty,min=1"
      mode:
        type: string
        description: >
          Competition participation mode. teams_only is the recommended default
          for serious team CTF events, and solo_only is for individual events.
        enum:
          - teams_only
          - solo_only
        x-oapi-codegen-extra-tags:
          validate: "omitempty,oneof=teams_only solo_only"
      name:
        type: string
      start_time:
        format: date-time
        type: string
    required:
      - name
    type: object
